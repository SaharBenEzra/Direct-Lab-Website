apiVersion: v2
name: direct-lab
description: Direct Lab application site + intake backend (Flask + MongoDB)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: mongodb
    version: "19.1.21"
    repository: "oci://registry-1.docker.io/bitnamicharts"
    condition: mongodb.enabled
