apiVersion: v1
kind: Service
metadata:
  name: {{ include "direct-lab.fullname" . }}
  labels:
    {{- include "direct-lab.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "direct-lab.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
